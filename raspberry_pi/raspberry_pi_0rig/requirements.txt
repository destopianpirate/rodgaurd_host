# Raspberry Pi Dependencies for RoadGuard
# Install with: pip install -r requirements.txt

# ─── Core ────────────────────────
ultralytics>=8.0.0          # YOLOv8 for pothole detection
opencv-python>=4.8.0        # Camera capture and image processing
numpy>=1.24.0               # Numerical operations

# ─── Hardware Drivers ────────────
pyserial>=3.5               # GPS module (NEO-6M) serial communication
adafruit-circuitpython-mlx90640>=1.3.0  # MLX90640 thermal camera (I2C)
RPi.GPIO>=0.7.1             # GPIO control for buzzer
adafruit-blinka>=8.0.0      # CircuitPython compatibility layer for Pi

# ─── Networking ──────────────────
requests>=2.31.0            # HTTP client to send data to Flask API

# ─── Optional (for GPS parsing) ──
pynmea2>=1.19.0             # Advanced NMEA sentence parsing (optional)
roboflow